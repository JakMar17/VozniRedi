import { NoOfStopsPipe } from './no-of-stops.pipe';

describe('NoOfStopsPipe', () => {
  it('create an instance', () => {
    const pipe = new NoOfStopsPipe();
    expect(pipe).toBeTruthy();
  });
});
